import React from "react";
import classes from "./order.module.css";

export default function Pending(){
    return (
        <div className={classes.pending}>
            <p>This is the pending functional components</p>
        </div>
    );
}